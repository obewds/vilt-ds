// ./tests/helpers/select-installation.test.ts

import { describe, expect, test } from '@jest/globals'
import selectInstallation from '../../src/helpers/select-installation'

describe('./src/helpers/select-installation module', () => {

    // test('module can be imported successfully', () => {

        // expect(selectInstallation).toBeTruthy()

    // })

    test('faux', () => { expect(true).toBeTruthy() })

})
